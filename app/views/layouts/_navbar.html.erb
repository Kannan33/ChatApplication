<nav class="navbar sticky-top navbar-expand-lg bg-light">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <a class="navbar-brand" href="#">Chat App</a>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_user&.present? %>
          <!-- Home Path -->
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>
          <!--  Profile Path  -->
          <li class="nav-item">
            <%= link_to "Profile", profile_users_path(current_user), class: "nav-link" %>
          </li>
          <!--  Profile Edit Path  -->
          <li class="nav-item">
            <%= link_to "Edit Profile", edit_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
      <!--  Search User From List  -->
      <% if current_user&.present? %>
        <div class="nav-item m-2">
          <%= current_user&.first_name + " " + current_user&.last_name  %>
        </div>
        <div class="nav-item m-2">
          <%= button_to(
                "Log Out",
                destroy_user_session_path,
                method: :delete,
                class: "btn btn-outline-danger"
              ) %>
        </div>
      <% else %>
        <div class="nav-item m-2">
          <%= link_to "Login", new_user_session_path, class: "btn btn-sm btn-outline-success" %>
        </div>
        <div class="nav-item m-2">
          <%= link_to "Register", new_user_registration_path, class: "btn btn-sm btn-outline-success" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
<!-- Search Navbar -->
<% if user_signed_in? %>
  <nav class="navbar bg-body-tertiary">
    <div class="container-fluid">
      <%= form_with(model: nil, url: profile_user_path(current_user), method: :get,local: false ,data: { turbo: false  }, class: "d-flex") do |f|%>
        <%= f.text_field :email, class: "form-control me-2", placeholder: "Search", type: "search", required: true %>
        <%= f.submit "Search", class: "btn btn-outline-success" %>
      <% end %>
    </div>
  </nav>
<% end %>